// type Product = {name: string, price: number}
// function print<T extends Product>(products: T[]):number {
//     return products.reduce((s, p) => s + p.price, 0)
// }


// const res = print<Product>([{name: 'a', price: 1}, {name: 'b', price: 2}]);
// console.log(res);
